@model PagedViewList<CoreSysBrand>
<div class="row" id="list">
    <table id="viewTable" class="table table-striped table-bordered table-hover dataTable">
        <thead>
            <tr role="row">
                <th>品牌</th>
                <th>集团</th>
                <th>模板ID</th>
                <th>代码</th>
                <th>LOGO</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Source)
            {

                <tr>
                    <td>
                        <input type="hidden" class="key" value="@item.Id" />
                        @item.BrandName
                    </td>
                    <td>
                        @UtilityHelper.GetGroupName(item.GroupId)
                    </td>
                    <td>
                        @item.TemplateId
                    </td>
                    <td>
                        @item.BrandCode
                    </td>
                    <td>
                        <img src="@item.Logo" />
                    </td>
                    <td>
                        <div class="visible-md visible-lg hidden-sm hidden-xs action-buttons">
                            <a data-optype="view" class="blue" href="javascript:void(0)">
                                <i class="icon-zoom-in bigger-130"></i>
                            </a>
                            <a data-optype="edit" class="green" href="javascript:void(0)">
                                <i class="icon-pencil bigger-130"></i>
                            </a>
                            <a href="@Url.Action("Index", "GlobalConfig", new { PermitionType = "Brand", TypeId = item.Id })">
                                配置项管理
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>